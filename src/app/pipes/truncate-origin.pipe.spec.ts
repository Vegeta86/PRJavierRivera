import { TruncateOriginPipe } from './truncate-origin.pipe';

describe('TruncateOriginPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateOriginPipe();
    expect(pipe).toBeTruthy();
  });
});
